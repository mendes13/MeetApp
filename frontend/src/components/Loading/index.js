import React from 'react';

import { LoadingWrapper, LoadingIcon } from './styles';

function Loading() {
  return (
    <LoadingWrapper>
      <LoadingIcon />
    </LoadingWrapper>
  );
}

export default Loading;
